# September 2026

> [!NOTE]
> **AI-Generated Content Disclosure**: This worklog is automatically generated and maintained by AI coding agents to document daily progress, debugging sessions, and technical decisions.

## 05/09/2026 — War Powers product experience and fork documentation

- Removed the browser campaign-unlock callback and all completion-based deployment gating: every authored scenario is immediately selectable, independent of browser progress records. Single-faction scenarios now hide the unsupported faction button and brief and identify the authored side in the existing ObjectiveHint; skirmish restores both choices. Deployment handlers accept only a real faction map path. The seven mission indices used by the browser journal are preserved, and in-match technology/funds/cooldown requirements are unchanged. The parent dataset supplies the two new faction hint strings. The WebAssembly build and source/diff checks pass. Ordinary browser validation on `4ebb625eb528` confirmed Op04 deploys with 0/4 campaign results, Op01 shows only the Meridian button and brief, Op02 shows only Jackal, and skirmish restores both factions. Final source candidate `66f4e32fcfab` uses the same native code.
- Extended the same corner padlock to the two disabled faction deployment text buttons after browser review found their neutral palette still resembled an enabled secondary action. Their native color/text renderer reuses the badge at the upper right, with no additional text or face dimming. Exact deployment window names exclude the selected mode tabs and other shell controls; the long deployment button layout leaves the centered labels clear. The WebAssembly build and diff checks pass. Candidate `1df03a19fdd9` visibly marks unavailable Jackal deployment, ignores its click, keeps Field Training selected without a lock, and deploys Meridian normally.
- Player feedback found unavailable portraits too similar to muted enabled artwork. Disabled War Powers command portraits now receive a pale padlock on an opaque dark corner badge, retaining a visible shape cue in grayscale; non-recharging unavailable portraits also use stronger dimming. The renderer checks native enabled/overlay state, a command/queue window name and local WP/WPJ faction. Empty queue slots have no badge, and populated cancel buttons remain enabled. Tooltip, availability and cooldown behavior are unchanged; the badge is drawn after the existing wedge/overlay passes. This shared Core renderer change is guarded away from retail datasets. The WebAssembly build passes. On `15907462d540`, normal training shows prerequisite and insufficient-funds locks, readable prerequisite tooltips, a $450 active-queue cancellation refund, blank empty slots and affordability locks clearing when money returns. On `1df03a19fdd9`, a normally built Directorate retains its recharge sweep/countdown and lock, clears the lock at POWER READY after natural recharge, accepts a ground target and restores the lock on the next cooldown. The final browser run reports no error-level logs.
- Selected completed Directorates and Dens now explain their native power state in the existing Orders heading: ready, remaining recharge time, power required or unavailable. The text follows the actual module ready frame and explicit disabled flags, resets to localized ORDERS for other selections, and uses the existing 536-pixel reference-width label. The parent dataset supplies the localized strings. The WebAssembly build passes; staged candidate `2a5fb93551f9` visibly displays RECHARGING 1:51 at 0:39 elapsed, within the existing heading bounds.
- Added a bounded review-only selected-power trace to distinguish charge state, disabled flags and button/pending-command state without forcing readiness. It records at most 64 lines per fresh review scene and uses public state; normal battles do not enter this path. The parent browser checks activated both powers on explored ground after natural initial waits. Build `9294283038c0` showed Meridian readiness at frame 4,505 against a 4,500 ready frame, actual button targeting, then a recharge reset and a new impact scorch after target input. The earlier failed input attempt was not reproduced; no object-target rejection bug is claimed. The separate missing `SpecialPowerCreate` data lifecycle hook is corrected in the parent dataset so normal construction starts recharge on completion; it is not claimed to explain the prebuilt review observation.
- Input QA found fast Ctrl+number chords selecting an empty group instead of creating one. The shared `Keyboard::updateKeys` applied the batch's final modifier state to every event, losing a Ctrl press/release completed within one update. It now captures modifier state in event order; synthesized repeats use current modifiers. The durable `scripts/qa/test-keyboard-modifiers.py` harness compiles the actual production update/repeat/translation methods against deterministic device/timer fixtures: the prior source failed 14 chord/order cases, while the revised source passed all 16 cases including held modifiers, both sides of Ctrl/Shift/Alt, combined modifiers and repeat release. Its single command, optional negative-control revision and integration limits are documented in CONTRIBUTING and the script index. This shared Core fix covers both games. The rebuilt browser candidate `db2a99b4c142` visibly assigned group 1 to nine selected units using Ctrl+1; recall and the broader input checks are recorded separately by the parent workspace.
- Resumed browser QA found native construction notices obscured by the browser objective panel. Routed the central `InGameUI` message path to the existing web notification callback only for active War Powers players, suppressing duplicate native drawing while retaining other-dataset/native-host fallback and the message visibility setting. Script objective notices retain their existing route. Removed the main-menu compiler timestamp because Settings already exposes the staged build ID. The rebuilt browser run observed a normally constructed Jackal Watchpost finish and display a centered Construction complete notice; the main-menu timestamp is absent as intended.
- Added a read-only engine-to-browser player state bridge with selected-unit identity/health, owned army/economy counts, headquarters health and script objective counters. Map and mission IDs come from the active map and match the parent workspace's operation metadata.
- Extended match results with operation ID, difficulty and outcome statistics. Added retry and change-battlefield actions, grouped deployment choices, faction-locked operations, map preview references and a browser-provided campaign unlock check. The journal can select an engine deployment screen without starting a match behind an active battle.
- Added owned web-panel pause/release and independent audio-channel settings through the existing audio API. War Powers scripted announcements use the browser text callback when present, retaining native fallback.
- Added fixed-slot native save/load exports. The parent web application owns IndexedDB mount/sync, compatibility metadata and durable-storage handling; serialization success alone is not a persistence guarantee.
- Browser integration checks confirmed a checkpoint survives a full browser reload and restores elapsed battle time, owned units and a constructed building. Restored portable map paths use lowercase, so the bridge now canonicalizes the pack's map IDs for objectives/results/retry. The load wrapper finishes the native control-bar entrance before a browser briefing can pause it off screen. Subsequent normal training save/reload checks restored canonical mission identity, stage, structures, time and the complete control bar; details are recorded in the parent verification document.
- The parent workspace now generates a 1280×720 reference HUD and menus with a compact command bar, separate command/queue hit areas, selected-unit status, economy/power, radar and previews. Its GameData viewport scale matches the bar. These dataset files belong to the parent workspace, not this engine repository.
- Kept construction coverage in `base`/`wedge`; unit/control diagnostic paths now log explicit factory/power fixtures to meet the new gameplay prerequisites and support both basic faction tanks. Production timeout uses the public player-aware build-time calculation.
- Added the IDBFS link dependency with the parent integration. Initial compiler feedback found private idle-count and protected raw build-time access; replaced them with owned-dozer idle queries and `calcTimeToBuild` rather than widening access.
- The first browser pass exposed the Emscripten scalar-argument limit in the state callback. Replaced the 31-argument call with a synchronous numeric snapshot and four string pointers, preserving the public state schema. The rebuilt bridge reached the shell and deployment screens; map-start validation continues with the War Powers dataset.
- Added environment-gated `economy` and `powers` mechanics diagnostics. Economy creates an explicitly logged depot fixture, produces its hauler through native queue messages, then reports cache stock, carried boxes and cash over 60 seconds. Powers creates logged technology/target/scout fixtures, advances readiness explicitly, invokes the real native power and reports target damage or new ambush troops plus cooldown. These do not cover construction, UI targeting, natural recharge timing or balance.
- Corrected the economy diagnostic after its first run: it must select a hauler produced by the fixture depot, excluding the map's opening hauler, and complete native `CreateModule::onBuildComplete` hooks so the depot registers with the resource manager. The initial diagnostic failure is not treated as evidence that normal player-built depots fail.
- Browser build `7a05419481b2` passed both corrected supply diagnostics: at 60 seconds the Meridian Porter had removed 20 crates and earned $1,100 including approximately $300 passive income; the Jackal Scavenger had removed 12 crates and earned $740 including approximately $240 passive income. Both carried their expected four/two crates and repeated deliveries. Native power fixtures also passed: Precision Strike dealt 550 damage and advanced its next-ready frame to 4,680; Tunnel Ambush created five infantry and advanced its next-ready frame to 3,780. These observations retain the fixture limitations above.
- Opening Porters and Scavengers placed in a map now enter native resource gathering on a fresh game, matching newly produced haulers. This hook names only the two War Powers templates and does not run during checkpoint restore.
- Native idle-builder/headquarters hints now read the active command map, so persisted key remaps are reflected after restart.
- Added a repeatable `WP_REVIEW_SCENE` developer hook on fresh Flats maps, with faction building/unit/wreck rows and normal creation completion. Its `stress` variant adds at most 120 mixed units across both players with native attack-move orders. It logs counts, observed timing and WebAssembly heap capacity for a bounded interval; these measurements do not assert visual quality or a performance release gate. Added an actual-result assertion for the separate mission-script diagnostic modes; fixture injections do not themselves record a victory.
- The training diagnostic reached an actual native victory in an earlier browser build. Its subsequent wait falsely failed because the result banner leaves the game active during cleanup; the final harness consumes the observed result instead, and accepts it only after the expected final fixture has armed the check. The parent subsequently ran all seven native wins, seven losses and three additional HQ-loss cases successfully on `6375e6bfb189`, asserting actual result callbacks.
- Reworked engine/DXVK entry documentation around the actual private forks, exact workspace build boundaries, preserved upstream attribution and explicit local-DXVK selection. Added fork contribution routing so inherited upstream examples cannot be mistaken for publication authorization. Normalized worklog index links to repository-relative paths.
- Read-only hygiene checks scanned 4,641 engine and 661 DXVK tracked text files for high-confidence private-key/token patterns, with no matches. Binary/large files were excluded, so this is a bounded check rather than a repository-wide security certification. No new absolute developer paths appeared in changed lines or new files; inherited path references outside this work were left intact.

Validation recorded at this point: the final WebAssembly target compiled and linked successfully, with inherited compiler warnings. The parent workspace staged candidate `dea34eeedcf7` (63.638 MiB) and reported its automated web-state, packaging, content-reference, voice and 17-map validation gates passing. Generated-window IDs are unique, all 18 command and nine queue windows remain, referenced UI text keys resolve, mission metadata IDs align, all 44 review-scene template names resolve, and engine diff whitespace checks pass. A final source review found no additional blocker in the native save/result/mission integration.

After the development Mac was unlocked, browser QA covered the mission outcome matrix, save/restore identity and control-bar placement, both native roster scenes, an isolated one-minute crowded battle, natural power targeting, control groups and settings/fullscreen/pause recovery. These local observations do not establish complete native-DXVK, cross-browser, human balance or release certification. Detailed browser evidence and its build identities live in the parent workspace's `docs/verification.md`. No deployment, publication, upstream communication or history rewriting was performed for this work.
